(ns ai.trader.app)

(defn main []
  (println "Hello shady world!"))

(inc 1)

(print "hello world!")

(main)

;; Automatically selecting app does not work for some reason with .dir-locals.el
;; There are some warnings when starting the REPL

